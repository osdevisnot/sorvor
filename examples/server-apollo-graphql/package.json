{
  "name": "server-graphql",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "sorvor src/server.js --platform=node --format=cjs",
    "start": "sorvor src/server.js --platform=node --format=cjs --serve"
  },
  "dependencies": {
    "apollo-server": "^2.21.0",
    "graphql": "^15.5.0"
  }
}
