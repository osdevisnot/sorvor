{
  "name": "preact-counter",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prebuild": "npm install",
    "build": "sorvor --loader:.js=jsx --jsx-factory=h",
    "start": "yarn build --dev"
  },
  "dependencies": {
    "preact": "^10.5.7"
  }
}
