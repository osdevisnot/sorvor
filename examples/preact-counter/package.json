{
  "name": "preact-counter",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "sorvor src/counter.js --minify --sourcemap=external --loader:.js=jsx --jsx-factory=h",
    "start": "sorvor public/index.html --dev --loader:.js=jsx --jsx-factory=h"
  },
  "dependencies": {
    "preact": "^10.5.7"
  }
}
