{
  "name": "preact-counter",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prebuild": "yarn install --silent",
    "build": "sorvor --loader:.js=jsx --jsx-factory=h",
    "prod": "yarn build --minify --sourcemap=external",
    "start": "yarn build --dev"
  },
  "dependencies": {
    "preact": "^10.5.7"
  }
}
