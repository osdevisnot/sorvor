{
  "name": "preact-counter",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "sorvor public/index.html --loader:.js=jsx --jsx-factory=h --minify --sourcemap",
    "start": "sorvor public/index.html --loader:.js=jsx --jsx-factory=h --serve",
    "dist": "sorvor src/counter.js --minify --sourcemap"
  },
  "dependencies": {
    "preact": "^10.6.4"
  }
}
